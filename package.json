{
  "name": "lib-cat",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bcrypt": "^1.0.3",
    "express": "^4.16.2",
    "mysql": "^2.15.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.1"
  },
  "proxy": "http://localhost:5000/",
  "scripts": {
    "client": "npm start",
    "server": "cd src/js && nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \" npm run server\" \"npm run client\"",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
